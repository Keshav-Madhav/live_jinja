// Default template and variables
export const defaultTemplate = `Hello {{ name }}!`;

export const defaultVars = {
    name: "World"
};

// CodeMirror editor options
export const commonEditorOptions = {
    lineNumbers: true,
    theme: 'material-darker',
    lineWrapping: true,
    scrollbarStyle: 'native'
};
